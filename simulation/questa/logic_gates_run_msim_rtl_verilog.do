transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog  -work work +incdir+C:/Digital\ Electronics/logic_gates {C:/Digital Electronics/logic_gates/logic_gates.v}

